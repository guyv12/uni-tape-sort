{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug tape-sort (manual)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/tape-sort",
      "cwd": "${workspaceFolder}",
      "MIMode": "gdb",
      "preLaunchTask": "CMake Build",
      "externalConsole": true
    }
  ]
}
